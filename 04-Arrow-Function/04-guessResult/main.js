// โค้ดทั้งสองชุดด้านล่างทำงานได้เหมือนกันหรือไม่

const logName = (name) => console.log(name);

const logName = (name) => console.log(name);

//The answer is YES,
// as long as you receive only one parameter
// if you do receive 2 parameters make sure you wrap the parentesis (),
// and after the arrow function you if you have bracket {}, dont forgot to specify Ruturn
